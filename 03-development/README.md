# 03 — Development

All application code goes here. Everything must match the **frozen API
contract** in `../02-design/api-contract-v1/` — that is the single source of truth.

Suggested layout:
- `frontend/` — React + Tailwind (candidate & recruiter web pages)
- `backend/`  — FastAPI + PostgreSQL (the API)
- `ai/`       — Whisper transcription + GPT-4o-mini scoring

_Placeholder so this folder appears on GitHub. Replace as code lands._
